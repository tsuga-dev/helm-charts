---
# Source: opentelemetry-kube-stack/templates/cluster-receiver.yaml
apiVersion: opentelemetry.io/v1beta1
kind: OpenTelemetryCollector
metadata:
  name: opentelemetry-kube-stak-opentelemetry-kube-stack-cluster-receiver
  labels:
    app.kubernetes.io/name: opentelemetry-kube-stack
    app.kubernetes.io/instance: opentelemetry-kube-stak
    app.kubernetes.io/component: cluster-receiver
spec:
  mode: deployment
  image: ghcr.io/open-telemetry/opentelemetry-collector-releases/opentelemetry-collector-k8s
  resources:
    limits:
      cpu: 500m
      memory: 512Mi
    requests:
      cpu: 100m
      memory: 128Mi
  serviceAccount: opentelemetry-kube-stak-opentelemetry-kube-stack
  env:
    - name: TSUGA_OTLP_ENDPOINT
      value: https://intake.astronomical-oak.tsuga.com:443/api/v1/otlp
    - name: MY_POD_IP
      valueFrom:
        fieldRef:
          apiVersion: v1
          fieldPath: status.podIP
    - name: TSUGA_API_KEY
      valueFrom:
        secretKeyRef:
          name: otel-secret
          key: TSUGA_API_KEY
  config:
    receivers:
      
      k8s_cluster:
        collection_interval: 10s
    exporters:
      otlphttp/tsuga:
        endpoint: ${TSUGA_OTLP_ENDPOINT}
        headers:
          Authorization: Bearer ${TSUGA_API_KEY}
    processors:
      
    service:
      pipelines:
        
        # Gateway pipelines
        metrics:
          receivers:
          - k8s_cluster
          exporters:
          - otlphttp/tsuga
        logs/entity_events:
          receivers:
          - k8s_cluster
          exporters:
          - otlphttp/tsuga
      telemetry:
