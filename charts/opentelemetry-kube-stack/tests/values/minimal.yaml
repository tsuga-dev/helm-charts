# Minimal configuration for testing
tsuga:
  otlpEndpoint: "https://test-endpoint.com"
  apiKey: "test-api-key"

secret:
  create: true

serviceAccount:
  create: true

rbac:
  create: true

agent:
  enabled: true

cluster:
  enabled: true
